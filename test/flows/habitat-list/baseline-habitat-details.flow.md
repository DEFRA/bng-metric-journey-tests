# Baseline Habitat Details User Flow

> **Moved — single source of truth.** The `/baseline-habitat-details` route (view / save /
> conditions-proxy) is documented canonically at
> [`../habitat-details/habitat-details.flow.md`](../habitat-details/habitat-details.flow.md),
> matching its spec domain (`test/specs/habitat-details/`).
>
> This stub remains because the baseline habitat list (this domain) links through to the
> habitat-detail edit page. See the canonical doc for the steps, validation, redirect anchors,
> and per-type strategy behaviour (area / hedgerow / watercourse). Do not duplicate that content
> here — keeping two copies is what let the docs drift out of sync with the service.
